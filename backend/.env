# IMPORTANT: Do NOT commit real secrets to this file.
# Copy from .env.example and fill in your real values locally only.
# This file is in .gitignore and should never be pushed to the repository.

# OpenAI API key (add your real key here locally, never commit)
OPENAI_API_KEY=

# MongoDB connection string (example for local MongoDB)
MONGO_URI=mongodb://127.0.0.1:27017/agronex

# Backend port
PORT=5000
